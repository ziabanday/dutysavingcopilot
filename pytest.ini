[pytest]
addopts = -ra
testpaths =
    tests
markers =
    offline: offline-safe tests that must not use network or external DBs
    online: tests that may hit network/services
    pgvector: tests that require a pgvector-backed store